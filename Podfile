# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Curator App' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Curator App
  pod 'Gloss', '~> 2.0'
  pod 'Alamofire', '~> 4.6'
  pod 'PromiseKit', '~> 6.0'
  pod 'Nuke'
  pod 'Fabric' 
  pod 'Crashlytics'

end
